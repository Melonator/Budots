[module/xwindow]
type = internal/xwindow

format = <label>
format-background = ${colors.black}
format-foreground = ${colors.white}

label = %{T3}%title%%{T-}
label-maxlen = 35

label-empty = %{T3}Desktop%{T-}
label-empty-foreground = ${colors.white}
